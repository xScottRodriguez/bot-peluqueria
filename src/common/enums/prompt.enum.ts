enum PROMPT {
  seller = "seller",
  generateFormatDate = "generate-format-date",
  schedule = "schedule-prompt",
  services = "services",
  Cancel = "cancel-appointment",
  generateJson = "generate-json-parse",
  specialSchedule = "special-schedule",
}

export { PROMPT };
